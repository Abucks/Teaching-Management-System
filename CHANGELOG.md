# 更新日志 · Changelog

本文件记录「教学管理系统」的所有重要变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 🔴 **重要** = 影响使用或必须升级的变更　🟠 **重点** = 主要新功能　🟢 一般改进 / 修复

---

## [1.4.0] — 2026-09-10

### 🟠 新增：学生气泡资料页（点开磁贴即可看全信息）

- **单击学生磁贴**即可滑入该生的「资料页」，把已编辑好的信息以**气泡/标签云**形式呈现，
  气泡**大小不一**（tag cloud 效果）：
  - 身份气泡：👤 姓名（最大）、🏫 班级、🆔 学号、🗓 考试次数
  - 🏷️ 标签气泡：**本班拥有该标签的人越多，气泡越大**（悬停显示占比）
  - ⭐ 头衔气泡（金色）
  - 成绩气泡：📊 平均分（按分数档放大/缩小）、⬆️ 最高、⬇️ 最低、各科目平均分
    （分数越高越大）、🕐 最近考试、💬 评语
- 资料页支持 ✏️ 编辑资料 / 📈 成绩详情 / 🗑 删除，并自动记入导航历史
  （面包屑「🏠 班级总览 › 📚 高一(1)班 › 👤 张三」，可点任意一段跳回）

### 🟠 新增：📋 学情管理模块（第 5 个模块）

- **自动读取已有学员名单**，每名学生一行（姓名/学号/班级为只读基础列）
- **表格内自由补充**学生任何信息：可自行**添加 / 重命名 / 删除字段（列）**，
  如家庭情况、薄弱科目、学习习惯、家长电话等，列数不限
- 单元格**随改随存**（底部显示自动保存次数），支持按关键词筛选行、刷新名单
- 支持**导出 Excel**（.xlsx）归档
- 🔴 **模块隔离（重要）**：学情信息**不会**出现在「学生管理」的学生磁贴、
  班级磁贴或气泡资料页中；删除学生时其学情填写值自动级联清理

### 🟢 修复

- **修复导入 xlsx 时空单元格被读成字符串「None」**：此前空标签会显示成「🏷️None」、
  空学号会变成「None」；现统一转成空值（空学号自动回退为学生姓名）
- 修复 Excel 数值型学号带多余小数的问题（`20260001.0` → `20260001`）
- 新增 `_cell_text()` 单元格文本规范化，学生表与成绩表解析共用

### 🟢 工程

- **新增多系统安装包流水线**：打 `v*` 标签自动构建并发布 Release
  - Windows：Inno Setup 安装向导(**Setup.exe**，按用户安装免管理员) +
    **MSI**（Python 标准库 msilib 生成，装 Program Files，支持主版本升级/阻止降级）+
    便携版 exe
  - macOS：`macos-14`(arm64) 与 `macos-15-intel`(x64) 双架构 DMG
  - Linux：**deb**(dpkg-deb) + **AppImage**(appimagetool)
  - 产物命名统一为 `TeachingManager-<版本>-<系统>-<架构>-<类型>-<UTC时间戳>`
  - 注：MSI 不用 WiX —— `dotnet tool install wix` 会装 v7，v7 要求接受 OSMF EULA，
    CI 上直接报 WIX7015；改用 Python 标准库 msilib 零依赖生成
- 新增学情数据表 `profile_fields` / `profile_values`（含索引），老数据库自动建表
- 测试规模提升至 **108 项逻辑 + 29 项界面/新功能 + 跨平台与防回归约束**，
  其中包含「学情信息不得出现在学生磁贴/气泡中」的**模块隔离回归测试**
  与「空单元格不得产生 None」的导入回归测试

## [1.3.1] — 2026-09-10

### 🔴 重要修复

- **修复「打开课程管理 / 切换页面时瞬间闪出多个程序窗口，随后全部关闭」的严重 BUG**
  - **根因**：日历格 `DayCell`、课程条目 `CourseRow`、各类磁贴、占位控件在构造时**未指定父控件**，Qt 会将其作为**顶层窗口（真实 OS 窗口）**创建；同时布局清理使用 `widget.setParent(None)`，又让控件**退化为顶层窗口**，且 `deleteLater` 延迟销毁导致越积越多（诊断时单次操作出现约 180 个 `DayCell` + 30 个 `QWidget` + 6 个 `CourseRow` 顶层窗口）。
  - **修复**：所有子控件构造时一律传入父控件；布局清理统一改为 `hide() + deleteLater()`，**彻底移除 `setParent(None)`**；覆盖学生/班级网格、月历、当天课表、便签墙、成绩磁贴全部模块。
  - **实测对比（真机 Windows GUI + 冻结 exe，打开课程管理瞬间的顶层窗口数）**：修复前 `6 → 30 → 70 → 归零`（闪烁）；修复后 **恒定 6**，无崩溃、无报错。
- **修复窗口标题重复显示**：Windows 上不再出现「教学管理系统 v1.3.1 - 教学管理系统」（`setApplicationDisplayName` 仅 macOS 设置）。

### 🟠 新增

- **学生管理页面导航栏**：
  - `← 上一页`：回到上一个浏览过的页面（无历史时自动隐藏并禁用）；
  - **可点击面包屑**：`🏠 班级总览 › 高一(1)班 › 🔍 搜索：张`，点击任意一段即可跳回该页（其后的历史自动丢弃）；
  - `🏠 班级总览`：一键回到首页并清空前进历史；
  - 支持班级详情 / 搜索结果等多种页面类型，历史深度上限 12。

### 🟢 工程

- 新增回归测试：**切换页面/重建磁贴不得产生游离顶层窗口**（含 6 个模块内容类控件白名单校验）。
- 新增源码级约束（AST 校验）：代码中**禁止出现 `setParent(None)`**；磁贴/课程行/日历格必须带父控件；清理必须 `hide + deleteLater`。
- 新增导航栏行为测试（上一页 / 面包屑跳转 / 历史清理）。

---

## [1.3.0] — 2026-09-10

### 🟠 界面重做（重点）

- **字体统一**：界面与图表优先使用 **MiSans**，macOS 回退**苹方 PingFang SC**、Windows 微软雅黑、Linux 思源黑体；支持把 `MiSans.ttf` 放入 `fonts/` 目录自动加载。
- **学生管理改为两级浏览**：班级总览（班级磁贴：人数 + 标签统计）→ 点入班级查看学生，切换带**滑动 + 视差过渡动画**。
- **修复姓名与图标重叠**：磁贴**头像在上、完整姓名显示在头像正下方**（自动换行不省略），并保证磁贴高度足够，不再相互挤压。
- **排课系统改为月历视图**：月历中**有课的日期显示「N 节课」徽标**并高亮（今天为红色虚线框）；**点选日期展开当天课程详细时间安排**（时间/科目/教师/地点/备注/时长），支持编辑与删除。
- **反馈系统重构为「便签墙」**：
  - 支持 **文字 / 表格 / 图片** 三种便签，**取消日期与范围筛选，统一展示**；
  - **剪贴板智能粘贴**：`Ctrl+V` 或「📋 粘贴新建」自动识别剪贴板内容（图片 → 图片便签、Excel 表格 → 表格便签、其他 → 文字便签）；
  - 便签支持 **📌 置顶**、配色、搜索、查看大图、复制内容；删除便签时自动清理图片文件。
- **每个磁贴都有编辑入口**：学生 / 班级 / 便签 / 成绩 / 当天课程均支持**悬停右上角 ✏️🗑 按钮**与**右键菜单**编辑删除；成绩磁贴的编辑会打开该生成绩表，可逐条新增/修改/删除。
- 学生管理新增**班级重命名**（批量更新该班学生）。

### 🟢 数据与兼容

- 数据库新增便签字段（`content_type / table_json / image_path / image_caption / color / pinned`），**老版本数据库自动迁移**（幂等 `ALTER TABLE`），旧便签读取时补默认值。
- 便签图片落盘到数据目录 `feedback_images/`，数据库仅存文件名，便于整体迁移与备份。
- 新增接口：`get_course_day_counts()`（月历标记）、`search_feedbacks()`、`set_feedback_pinned()`、`delete_grades_by_student()`。

---

## [1.2.0] — 2026-09-09

### 🟠 跨平台版（macOS 适配）

- **数据目录自动定位**：`TMS_DATA_DIR` → 程序目录（便携模式）→ 用户数据目录（macOS 为 `~/Library/Application Support/TeachingManager/`），解决双击 `.app` 工作目录为 `/`、包内只读导致无法建库的问题；菜单新增**文件 → 打开数据目录**。
- **macOS 打包流水线**：`packaging/macos/` 提供一键脚本（依赖 → 图标 → `.app` → 代码签名 → `.dmg`）、PyInstaller 规格、图标自动生成（无需素材）、GitHub Actions 云端构建工作流与中文文档。
- macOS 细节适配：苹方字体、原生菜单角色（`QuitRole`/`AboutRole`）、Retina 高DPI、窗口按可用区域定位、`Info.plist` 完整配置。
- `.doc` 在非 Windows 平台给出明确提示（请转存 `.docx`），不再崩溃。
- Windows 端补齐应用图标（exe 图标 + 窗口图标）。

---

## [1.1.0] — 2026-09-09

### 🟠 单个 / 批量删除

- 四大模块新增删除能力：**勾 1 个即单个删除，勾多个即批量删除**，支持关键词过滤、全选、清空、已选计数与二次确认。
- **删除学生自动级联清理其名下全部成绩**；课程/反馈/成绩支持批量清理。
- 磁贴右键支持单条删除（学生、反馈、课程）。

### 🟢 修复与优化

- 修复图表中文显示为方块（matplotlib 字体栈未包含中文字体）。
- 修复成绩表 Excel **真实日期单元格**被解析成 `2026-06-01 00:00:00`、空白日期变成字符串 `None` 的问题。
- 优化学生导入去重性能（由逐行全表扫描 O(n²) 改为一次性快照 + 内存集合）。

---

## [1.0.0] — 2026-09-09

### 🟠 首个版本

- 四大模块：**学生管理**（磁贴展示）、**课程管理**（时间轴拖拽）、**反馈系统**（按时间/班级/类别筛选）、**成绩管理**（统计图表 + 个人成绩报告）。
- 支持导入 `.xlsx / .xls / .docx / .csv` 学生表与成绩表；SQLite 本地存储；统计图表（分数分布 / 科目对比 / 趋势）；学生个人成绩报告；中文界面与主题配色。
